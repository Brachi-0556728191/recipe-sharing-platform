 // ═══════════════════════════════════════════════════════════════
  // 🧹 Cleanup - ניקוי משאבים (למניעת Memory Leaks)
  // ═══════════════════════════════════════════════════════════════

  /**
   * Subject לניקוי אוטומטי של כל ה-subscriptions
   * 
   * איך זה עובד?
   * 1. כל subscribe מקבל .pipe(takeUntil(this.destroy$))
   * 2. ב-ngOnDestroy אנחנו קוראים ל-destroy$.next()
   * 3. takeUntil רואה את זה ומבטל את כל ה-subscriptions
   * 
   * למה זה חשוב?
   * - מונע Memory Leaks (דליפות זיכרון)
   * - מונע שגיאות (עדכון קומפוננטה שכבר לא קיימת)
   * - Best Practice בתעשייה
   */